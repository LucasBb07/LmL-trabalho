<%- include('cabecalho.ejs') %>
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Menu Administrativo</title>

  <!-- FontAwesome -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" />

  <!-- Fonte Poppins -->
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet" />

  <!-- CSS customizado -->
  <link rel="stylesheet" href="/css/style.css" />
</head>

<body>
  <header>
    <h1>🏆 Classificação do Torneio</h1>
  </header>

  <nav>
    <ul>
      <li><a href="/">Início</a></li>
      <li><a href="/times">Times</a></li>
      <li><a href="/partidas">Partidas</a></li>
      <li><a href="/jogadores">Jogadores</a></li>
      <li><a href="/sobre">Sobre</a></li>
    </ul>
  </nav>

  <main class="container">
    <table>
      <thead>
        <tr>
          <th>Posição</th>
          <th>Time</th>
          <th>Pts</th>
          <th>Vitórias</th>
          <th>Empates</th>
          <th>Derrotas</th>
          <th>GP</th>
          <th>GC</th>
          <th>Saldo</th>
        </tr>
      </thead>
      <tbody>
        <% if (classificacao && classificacao.length > 0) { %>
          <% classificacao.forEach((time, i) => { %>
            <tr>
              <td><%= i + 1 %>º</td>
              <td><%= time.nome %></td>
              <td><%= time.pontos %></td>
              <td><%= time.vitorias %></td>
              <td><%= time.empates %></td>
              <td><%= time.derrotas %></td>
              <td><%= time.golsPro %></td>
              <td><%= time.golsContra %></td>
              <td><%= time.saldo %></td>
            </tr>
          <% }) %>
        <% } else { %>
          <tr>
            <td colspan="9" style="text-align:center;">Nenhum time cadastrado ainda.</td>
          </tr>
        <% } %>
      </tbody>
    </table>
  </main>
</body>
</html>
